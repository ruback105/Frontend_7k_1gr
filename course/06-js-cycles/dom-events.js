// get single element by id
// const input = document.getElementById("click-me");
// const output = document.getElementById("output");

// triggers before input value change (keypress/keydown)
// input.addEventListener("keypress", (e) => {
//   output.innerHTML = e.target.value;
// });

// triggers after input value change (input/keyup)
// input.addEventListener("input", (e) => {
//   output.innerHTML = e.target.value;
// });

// get array of elements by className
// const paragraphs = document.getElementsByClassName("paragraph");

// input.addEventListener("input", (e) => {
//   Array.from(paragraphs).forEach((singleParagraph) => {
//     singleParagraph.innerHTML = e.target.value;
//   });
// });

// query selector (return first matching element)
// const queryParagraph = document.querySelector(".test.paragraph");

// console.log(queryParagraph.classList);

// query selector all (return matching elements array)
// const queryParagraph = document.querySelectorAll(".test.paragraph");

// console.log(queryParagraph);
